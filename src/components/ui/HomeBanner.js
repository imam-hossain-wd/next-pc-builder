import ComponentsCard from './ComponentsCard';

const HomeBanner = ({ components }) => {

  return (
    <section className="my-8 grid-cols-2">
        home banner
   
    </section>
  );
};

export default HomeBanner;

